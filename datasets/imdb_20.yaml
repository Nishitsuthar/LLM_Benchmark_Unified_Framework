name: imdb_20
description: "IMDb 20-movie subset — 10 questions, visual PDF evidence"

evidence_type: pdf_visual     # routes to VisualPdfEvidenceBuilder
eval_strategy: partial_credit  # routes to PartialCreditEvaluator

evidence_path: evidence/imdb_20/report.pdf
questions_file: evidence/imdb_20/questions.json
ground_truth_dir: ground_truth/imdb_20/

# Optional: prefix used to find dataset-specific prompt files in prompts/.
# Defaults to the dataset name. Set this to share prompts across datasets
# or if your prompt files use a different naming prefix.
# prompt_prefix: imdb_20
