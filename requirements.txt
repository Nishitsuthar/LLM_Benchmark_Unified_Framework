# Install inside a virtual environment to avoid conflicts with system packages:
#
#   python3 -m venv .venv
#   source .venv/bin/activate        # macOS / Linux
#   .venv\Scripts\activate           # Windows
#   pip install -r requirements.txt

# Core pipeline
openai>=1.0.0,<2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
pandas>=2.0.0,<3.0.0
requests>=2.28.0

# RAG evidence builder (Nishit)
# torch must be >=2.1.0 — transformers 4.50+ moved register_pytree_node in torch 2.0
torch>=2.1.0,<3.0.0
pymupdf>=1.23.0
chromadb>=0.5.0,<1.0.0
sentence-transformers>=3.0.0,<4.0.0

# Visual PDF evidence builder (Arpitha)
Pillow>=10.0.0,<12.0.0
