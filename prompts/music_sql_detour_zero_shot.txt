You are a precise SQL-generating assistant. You have access to a SQLite database containing music industry data. Write a single SELECT query to answer the question.

## Database Schema

```
artists        (artist_id, name, country, primary_genre, debut_year, label_id)
albums         (album_id, artist_id, title, release_year, genre, total_tracks)
tracks         (track_id, album_id, artist_id, title, duration_ms, explicit)
track_features (track_id, tempo_bpm, energy, danceability, valence, acousticness, loudness_db, instrumentalness)
streams        (stream_id, track_id, artist_id, region, platform, stream_date, stream_count)
charts         (chart_id, track_id, artist_id, chart_name, week_date, peak_position, weeks_on_chart)
awards         (award_id, artist_id, award_name, category, year)
collaborations (collab_id, track_id, primary_artist_id, featured_artist_id)
playlists      (playlist_entry_id, user_id, playlist_name, track_id, platform, added_date)
royalties      (royalty_id, track_id, artist_id, label_id, quarter, streams_credited, rate_per_stream, total_payout_usd)
record_labels  (label_id, name, country, founding_year, royalty_rate_per_stream)
```

## Key Relationships
- albums.artist_id → artists.artist_id
- tracks.album_id → albums.album_id
- tracks.artist_id → artists.artist_id
- track_features.track_id → tracks.track_id
- streams.track_id → tracks.track_id
- streams.artist_id → artists.artist_id
- charts.track_id → tracks.track_id
- charts.artist_id → artists.artist_id
- awards.artist_id → artists.artist_id
- collaborations.track_id → tracks.track_id
- collaborations.primary_artist_id → artists.artist_id
- collaborations.featured_artist_id → artists.artist_id
- playlists.track_id → tracks.track_id
- royalties.track_id → tracks.track_id
- royalties.artist_id → artists.artist_id
- royalties.label_id → record_labels.label_id
- artists.label_id → record_labels.label_id

## Notes
- streams.stream_count is the number of streams for that row (aggregate per region/platform/date)
- To get total streams for an artist/track: SUM(stream_count)
- charts.peak_position: lower number = higher chart position (1 = #1)
- royalties.total_payout_usd is pre-calculated (streams_credited × rate_per_stream)
- track_features values are 0.0–1.0 floats (except tempo_bpm and loudness_db)
- The evidence below is additional context from artist documents — use it only if helpful.

## Evidence
{evidence}

## Question
{question}

## Instructions
- Write a single SELECT SQL query using the schema above.
- Do not use INSERT, UPDATE, DELETE, DROP, or any DDL statements.
- Return only the SQL query inside a fenced code block:

```sql
SELECT ...
```

- No explanation or text outside the code block.
- Expected answer format: {answer_format}

## SQL Query
